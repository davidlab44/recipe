package com.david.epay.ui.screens.detail

import androidx.compose.material.TopAppBar
import androidx.compose.material3.Scaffold
import androidx.compose.material3.Text
import androidx.compose.runtime.Composable
import androidx.compose.runtime.remember
import com.david.epay.ui.screens.shared.ArrowBackIcon
import com.david.epay.ui.screens.shared.Thumb

@Composable
fun DetailScreen(recipeId: Int) {
    //val mediaItem = remember { getMedia().first { it.id == recipeId } }

    /*
    Scaffold(
        topBar = {
            TopAppBar(
                title = { Text(text = mediaItem.title) }
                //,navigationIcon = { ArrowBackIcon(onUpClick) }
            )
        }
    ) {
        //Thumb(mediaItem = mediaItem)
    }

     */
}